# 环境体检报告（对照《第三版执行计划》环境基线）

> **结论一句话（虚拟机平台复检后修正，见第七节）**：本机**具备可用的 Linux 运行时**——**VMware Workstation 17.6 + Ubuntu 24.04.3 虚拟机（正在运行）**，因此 **Wave 0 与 Wave 1 并非"完全阻塞"**；首版报告"没有任何 Linux 运行时"系**漏检非标准安装路径**所致。环境仅剩版本偏差（VM 为 Ubuntu 24.04.3，基线为 22.04）与资源余量偏紧两类问题。WSL2 与 Docker 仍未安装。
>
> 首版结论（已被第七节推翻，保留以供追溯）：~~当前开发机远不符合计划要求——没有任何 Linux 运行时（WSL 未安装、无 Docker、无 Linux VM），因此 Wave 0 与 Wave 1 完全阻塞。可用部分只有 Windows 侧的 C++/CMake/Node 工具链与一个版本偏低的 Redis。~~
>
> **取证方式**：全部结论来自本轮实机命令输出（见各节「证据」）。未取到证据的项标注为"未验证"。

---

## 一、逐项对照表

| # | 计划要求（环境基线） | 实测结果 | 结论 |
|---|---|---|---|
| 1 | OS: Ubuntu 22.04 LTS（内核 5.15） | 宿主 Windows 11 家庭版 Build 26200；Linux 侧为 **Ubuntu 24.04.3**（见七） | ⚠️ 版本偏差 |
| 2 | 可用的 Linux 运行时（WSL2 / Docker / 远程 VM） | **有 VMware Workstation 17.6 + Ubuntu 24.04.3 VM（运行中）**；WSL2/Docker 无（详见二·七） | ✅ 具备（走 VM 路径） |
| 3 | g++ 11.4（glibc 2.35） | MinGW-W64 g++ **16.1.0**（Windows 版，非 Linux 工具链） | ❌ 不符 |
| 4 | CMake 3.22+ | **4.3.3** | ✅ 符合 |
| 5 | Redis 7.x | **Redis for Windows 5.0.14.1**（standalone） | ❌ 版本偏低 |
| 6 | MySQL 8.x | **未安装** | ❌ 缺失 |
| 7 | libseccomp-dev | 无（且仅 Linux 提供） | ❌ 缺失 |
| 8 | libcap-dev | 无 | ❌ 缺失 |
| 9 | libargon2-dev | 无 | ❌ 缺失 |
| 10 | libhiredis-dev | 无 | ❌ 缺失 |
| 11 | redis-plus-plus | 无 | ❌ 缺失 |
| 12 | 内核能力：unshare / cgroup v2 / seccomp / pivot_root | Ubuntu 24.04.3 VM 内**待实测**（VM 已具备，尚未进入核对） | ⏳ 待验证 |

**统计（复检后）**：符合 2 项（#2 走 VM 路径、#4）；待 VM 内验证 1 项（#12）；不符/缺失 9 项。
>
> **表注**：首版对照的是"宿主 Windows"环境；复检后，Linux 侧基线（#3、#5~#12）应在 **Ubuntu 24.04.3 虚拟机内**重新核对——宿主上"缺失"不代表 VM 内没有（详见第七节）。

---

## 二、Linux 运行时（阻塞项，最关键）

**证据 1** — `wsl.exe -l -v` / `wsl.exe --status` 输出：

```
未安装适用于 Linux 的 Windows 子系统。可通过运行 “wsl.exe --install” 进行安装。
有关详细信息，请访问 https://aka.ms/wslinstall
```

> 注意：是**整个 WSL 子系统未安装**（不是"装了但没发行版"）。

**证据 2** — `command -v docker` → 无；`/c/Program Files/Docker/Docker/resources/bin/` → 不存在。→ **Docker 未安装**。

**证据 3（已被第七节推翻）** — 首版仅 `ls "/c/Program Files (x86)/VMware"` → 只见 `VMware VIX`（API 层），据此判"无可用的桌面虚拟机"。**该判断错误**：VMware Workstation 17.6 实际安装在**非标准路径 `D:\vm17.6\`**，首版未覆盖。三平台完整实测见第七节。

**证据 4** — WSL 功能开关查询（`Get-WindowsOptionalFeature`）返回「**该操作需要提升**」（需管理员），故功能开关状态**未取到**；但由证据 1 已可判定 WSL 未安装。

**旁证** — 虚拟化平台：`HypervisorPresent=True`（有 hypervisor 在运行，对安装 WSL2 是**有利**信号）。

> **影响（修正）**：计划 Wave 0 的 8 条冒烟测试（`unshare` / cgroup v2 / seccomp / `pivot_root`）可在 **Ubuntu 24.04.3 虚拟机**内执行——前提是该 VM 内已装齐基线库（**尚未验证**，见第七节待办）。首版"一条都跑不了"仅在"无 Linux 运行时"前提下成立，该前提已不成立。

---

## 三、Windows 侧现有工具链（部分可用）

| 工具 | 版本 / 路径 | 对本项目的意义 |
|---|---|---|
| gcc / g++ | 16.1.0，`/c/mingw64/bin/`（MinGW-W64 ucrt posix-seh） | 仅能编译 **Windows** 程序；**不能**替代 Linux g++（ABI/平台不同，且无 glibc 2.35） |
| cmake | 4.3.3，`/c/mingw64/bin/cmake` | ✅ 满足 3.22+ |
| ninja | 1.13.2 | ✅ 可用作构建后端 |
| make | 未安装 | 需补（或统一用 ninja） |
| node / npm | v24.18.0 / 11.9.0 | ✅ **Wave 4 前端可先在 Windows 上做** |
| python | 3.7.9（偏旧） | 工具脚本可用，注意 3.7 语法上限 |
| git | 2.54.0.windows.1 | ✅ |

---

## 四、Redis（在跑，但版本是坑）

**证据 5** — `D:\redis\` 为 **tporadowski/redis 的 Windows 移植版**，`RELEASENOTES.txt` 载明：

```
2021-10-18: Redis 5.0.14 for Windows
https://github.com/tporadowski/redis/releases/tag/v5.0.14
```

**证据 6** — 实例正在运行（`netstat`：`127.0.0.1:6379 LISTENING`，需认证）。用本机配置口令 `INFO server` 复核：

```
redis_version:5.0.14.1
redis_mode:standalone
os:Windows
arch_bits:64
```

**证据 7** — 关键命令存在性实测：

| 命令 | 结果 | 说明 |
|---|---|---|
| `XADD` | 存在 | Streams 基础（5.0+）|
| `XGROUP` / `XREADGROUP` / `XPENDING` / `XCLAIM` | 存在 | 消费组 + PEL 可用 |
| **`XAUTOCLAIM`** | **`ERR unknown command 'xautoclaim'`** | **不存在——该命令是 Redis 6.2 才引入** |

> **对计划的影响（重要）**：计划**任务 2.2「XAUTOCLAIM 10 分钟窗口」在本机 Redis 上无法实现**。
> 两条出路：
> 1. **升到 Redis 7.x**（推荐，随 WSL 一起装）——任务 2.2 原样可用；
> 2. 若坚持留在 5.0：把 2.2 改为「`XPENDING` 扫描 idle 超阈值 → 用 `XCLAIM` 认领」——5.0 可行，但代码更啰嗦、扫描更重。

**证据 8** — MySQL：无 `mysqld`/`mariadb` 命令、无 `/c/Program Files/MySQL`、`3306` 无监听 → 未安装。

---

## 五、恢复路径（按推荐度排序）

### 方案 A：WSL2 + Ubuntu 22.04（推荐）

```powershell
# 管理员 PowerShell
wsl --install -d Ubuntu-22.04
# 重启后进入 Ubuntu，安装依赖：
```

```bash
sudo apt update
sudo apt install -y build-essential cmake ninja-build redis-server mysql-server \
                    libseccomp-dev libcap-dev libargon2-dev libhiredis-dev
```

- 优点：最接近原生 Ubuntu，命令行体验与计划一致；`HypervisorPresent=True` 说明条件具备。
- 代价：需**管理员权限 + 重启**。
- 装完须在 **WSL 内**重跑本报告的 1/5/6/7/8 项复核（Windows 侧的结论不迁移）。

### 方案 B：Docker Desktop

- 适合 Wave 5 部署联调；但**容器内再建 cgroup 子树需 `Delegate=yes` 或 `--privileged`**，不适合 Wave 0 早期验证。

### 方案 C：VMware + Ubuntu VM ★已具备

- **本机已装 VMware Workstation 17.6（`D:\vm17.6\`）并有一台运行中的 Ubuntu 24.04.3 VM**——无需再搭建（详见第七节）。
- 该 VM 可直接作为 Wave 0/1 的目标 Linux 环境；若仍希望对齐 22.04，可在同一 Workstation 里再建一台 22.04 VM，或在现有 VM 内做版本核对后直接采用。

---

## 六、下一步（复检后修订）

1. **不再新建环境**：直接采用现成的 **方案 C**（VMware + Ubuntu 24.04.3 VM）。原推荐的方案 A（WSL2）仍可作为备选，但非必需。
2. 进入 Ubuntu VM，**核对工具链与内核能力**（脚本见第七节「待用户执行」）——这是进入 Wave 0 前的唯一硬门禁。
3. VM 内 Redis 若为 5.x，升到 7.x（否则任务 2.2 的 `XAUTOCLAIM` 不可用）；MySQL 同理确认 8.x。
4. 处理第七节列出的偏差（VM 规格、磁盘余量、Ubuntu 24.04 的 userns 限制）。
5. 把核对结果回填到《第三版执行计划.md》的「零点五」与 Wave 0 验收表。

> **未验证声明**：本报告未在 Linux 环境跑过任何冒烟测试；Wave 0 的 8 条内核能力对照在本机 **尚未执行**（Ubuntu VM 虽已运行，但未进入其内部核对）。首版"不可执行"的前提（当时误判无 Linux 运行时）已不成立，现应改为"**待在该 VM 内执行**"。

---

## 七、虚拟机平台专项复检（★修正首版结论）

> **复检动机**：首版报告断言"无可用的桌面虚拟机"，但该结论仅基于 `C:\Program Files (x86)\VMware` 一个路径。本轮改用**注册表 + 全盘 `.vmx` 检索 + `vmrun` 实机接口**三重取证，结论被推翻。

### 7.1 三平台实测

| 平台 | 状态 | 关键证据 |
|---|---|---|
| **VMware Workstation 17.6.0** | ✅ **已安装并运行** | 注册表 `DisplayIcon=D:\vm17.6\vmware.exe`；`FileVersion 17.6.0 build-24238078`；服务 `VMAuthdService` / `VMnetDHCP` / `VMware NAT Service` 均 **Running**（Automatic） |
| └ 已注册虚拟机 | ✅ 2 台 | `D:\Ubuntu24.04.3\Ubuntu24.04.3\Ubuntu24.04.3.vmx`（**运行中**）、`D:\centos\CentOS 7 64 位.vmx` |
| **VirtualBox** | ❌ 未安装 | `Program Files\Oracle\VirtualBox` 不存在；无 `VBoxManage`；无相关服务 |
| **Hyper-V** | ❌ 不可用 | 无 `vmms.exe`/`vmcompute.exe`；无 `vmms` 服务；宿主为 **Windows 11 家庭版**（不提供 Hyper-V 角色） |
| WSL2 | ❌ 未安装 | `wsl --status` → "未安装适用于 Linux 的 Windows 子系统" |
| Docker / Podman | ❌ 未安装 | 无命令、无目录、无服务 |

> 旁证：`HypervisorPresent=True` 来自 **VBS/HVCI**（`VirtualizationBasedSecurityStatus=2`，`SecurityServicesRunning={2}`），**不是** Hyper-V。

### 7.2 Ubuntu 24.04.3 VM 配置（`vmrun list` 确认运行中）

| 项 | 值 |
|---|---|
| guestOS | ubuntu-64（virtualHW.version = 21） |
| vCPU | 2（`cpuid.coresPerSocket=1`） |
| 内存 | 4096 MB |
| 磁盘 | 约 21 GB（`-s001`~`-s011` 分片 vmdk） |
| guest 自报 | Ubuntu 24.04.3 LTS (Noble Numbat)，`kernel 7.0.0-28-generic` |
| 网络 | VMware Tools 运行（`vmrun getGuestIPAddress` 成功 → **192.168.31.11**）；从宿主 ping/端口探测**不可达** |
| 运行态 | 宿主进程 `vmware-vmx`（PID 2096）占用 **约 4.2 GB** 内存 |

### 7.3 宿主资源现值（VM 运行中）

| 资源 | 现值 | 风险 |
|---|---|---|
| 物理内存 | 15.8 GB 总，**仅余 0.6 GB** | 已接近耗尽；VM 独占约 4.2 GB |
| D: 盘 | 311 GB，**仅余 9.5 GB（97% 已用）** | VM 磁盘 + 编译产物增长空间紧张 |
| C: 盘 | 166 GB，余 17 GB（91% 已用） | 同样偏紧 |
| VBS/HVCI | 运行中 | VMware 走 Windows Hypervisor Platform 兼容模式，性能有降级 |
| 宿主 Redis | `127.0.0.1:6379` LISTENING | 仍为 Windows 版 5.0.14.1（首版结论沿用） |

### 7.4 对照《第三版执行计划》「零点五」

| 方案 | 计划定位 | 本机现实 |
|---|---|---|
| A. WSL2（Ubuntu 22.04） | **计划推荐** | ❌ 未安装 |
| B. Docker Desktop | 适合 Wave 5 | ❌ 未安装 |
| C. VMware + Ubuntu VM | 退路 | ✅ **已具备（Ubuntu 24.04.3，正在运行）** |

**判定**：本机**符合开发计划的环境前提**——已具备一个可用的 Linux 运行时（方案 C）。首版"Wave 0/1 完全阻塞"的结论**不成立**。

### 7.5 偏差与待办

1. **版本偏差**：基线为 Ubuntu 22.04（g++ 11.4 / glibc 2.35），现为 **24.04.3**（内核 7.0.0-28 / g++ 13.3.0）→ **已实测接受**（见 7.6）；仍需在计划「零点五」记一笔——白名单靠 strace 实测，不绑定编译器版本。
2. **VM 规格偏低**：2 vCPU / 4 GB。判题沙箱 + Redis + MySQL + g++ 同跑吃紧，建议视宿主余量升配（宿主 16 GB，当前仅余 0.6 GB，升级前需先释放其他占用）。
3. ~~**Ubuntu 24.04 的 userns 限制**~~ ✅ **已实测排除**：`sudo unshare --mount --pid --net --fork --mount-proc /bin/true` → `exit=0`，root 路径不受非特权 userns 的 AppArmor 限制影响。
4. **磁盘告急**：宿主 D 盘仅余 9.5 GB，为硬约束（VM 内 `/` 尚有 22 GB 可用）。
5. ~~**VM 内工具链未验证**~~ ✅ **已实测全部就绪**：g++ 13.3.0 / cmake 3.28.3 / strace 6.8 / Redis 7.0.15（`XAUTOCLAIM` 可用）/ MySQL 8.0.46 / redis-plus-plus 1.3.15 / 六个 `lib*-dev` 齐备。

### 7.6 VM 内验收实测（✅ 已执行，12/12 全绿）

原「待用户执行」脚本已在 VM 内跑完，结果如下（明细见《环境配置.md》第 10 节）：

| 类别 | 实测结果 |
|---|---|
| 系统 | Ubuntu 24.04.3 LTS (noble)，内核 7.0.0-28-generic |
| 编译器 | g++ **13.3.0** |
| 构建 | CMake **3.28.3** + ninja |
| 追踪 | strace **6.8** |
| 依赖库 | libseccomp-dev 2.5.5 / libcap-dev 2.66 / libargon2-dev 20190702 / libhiredis-dev 1.2.0 / libssl-dev 3.0.13 / nlohmann-json3-dev 3.11.3 |
| Redis | **7.0.15**，`XAUTOCLAIM` 可用（任务 2.2 原样可实现） |
| MySQL | **8.0.46** |
| redis-plus-plus | **1.3.15**（`.a` + `.so`） |
| cgroup v2 | `stat -fc %T /sys/fs/cgroup` → `cgroup2fs` |
| namespace | `sudo unshare --mount --pid --net --fork --mount-proc` → **`exit=0`** |
| LSM | `lockdown,capability,landlock,yama,apparmor,ima,evm`（**landlock 在列**） |
| 资源 | 内存 3.8 GiB（available ~2.0）；根分区 40 G，可用 22 G（43%） |

> **验证声明（第七节）**：7.2 的 VM 规格取自 `.vmx` 与 `vmrun`；**7.6 的工具链与内核能力为 VM 内实机执行输出**，已逐项核销。7.1 的平台结论来自宿主实机命令。
